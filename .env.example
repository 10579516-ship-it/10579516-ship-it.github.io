# Ghost Editor is fully static.
# The API key is entered in the in-app Settings modal and stored in localStorage.
# No environment variables are required for build or runtime.
# This file exists only to silence "missing .env" warnings on some systems.
